import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  logo: {
    width: 50,
    height: 50,
    marginBottom: 30,
    resizeMode: 'contain',
  },
});

export default globalStyles;

